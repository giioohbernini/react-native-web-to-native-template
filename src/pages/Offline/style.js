import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#002740',
    justifyContent: 'center',
  },
  title: {
    color: '#00cbcb',
    fontSize: 38,
    textAlign: 'left',
    fontWeight: '700',
  },
  subtitle: { color: '#ffffff', fontSize: 24, textAlign: 'left' },
  desc: { color: '#ffffff', fontSize: 18, textAlign: 'left', marginTop: 20 },
})

export default styles
